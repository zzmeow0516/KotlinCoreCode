1.
扩展函数作用：
不修改某个类的源码的情况下，仍然能够打开某个类，为某个类增加函数。

------------------------------------------------------

二. 基础语法
2.1
没有被声明返回值的，默认会返回Unit的类型（类似于void）

2.2
var = variable + final
对于var声明的变量，其引用不可以改变，但是引用的对象可以改变。